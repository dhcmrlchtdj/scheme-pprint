open! Containers

(* type call_frames = call_frame list *)
(* and call_frame = (next_expression * environment * (argument list) * next_frame) *)

(* let environment = [] *)

(* let call_frame = [] *)
(* let control_stack =  [] *)

(* let closure = [] *)
(* let continuations = [] *)

let run (insts : Instruction.t list) : string = failwith "TODO"
